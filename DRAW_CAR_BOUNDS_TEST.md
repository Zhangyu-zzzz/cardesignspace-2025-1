# 画个车 - 像素级边界检测测试指南

## 🧪 测试步骤

### 1. 访问画个车功能
```
http://localhost:8080/#/draw-car
```

### 2. 创建测试载具

#### 测试用例 1: 细长的载具（如火箭）
- 画一个竖直的火箭形状
- 查看在车库中是否紧贴上下边界
- 左右方向应该有较大的活动空间

#### 测试用例 2: 扁平的载具（如赛车）
- 画一个水平的赛车形状
- 查看在车库中是否紧贴左右边界
- 上下方向应该有较大的活动空间

#### 测试用例 3: 不规则载具
- 画一个L形或不规则形状
- 查看边界是否紧贴实际绘制内容

### 3. 启用调试模式

在车库界面按 **`D`** 键

**预期效果**:
- 显示提示: "调试模式已启用 - 可以看到精确的矩形边界"
- 每个载具显示蓝色矩形边框
- 边框应该紧贴实际绘制内容
- 中心有红色圆点

### 4. 验证边界精准度

#### 页面边界测试
- ✅ 载具应该能够**紧贴**页面四边
- ✅ **几乎没有间隙**（最多2px）
- ✅ 不应该看到载具离边界很远就反弹

#### 载具间碰撞测试
- ✅ 两个载具碰撞时应该是**边界实际接触**
- ✅ 不应该看到明显的空隙
- ✅ 碰撞方向应该符合视觉预期（垂直碰或水平碰）

#### 点击测试
- ✅ 点击载具的绘制内容能够选中
- ✅ 点击载具周围的空白区域不会选中
- ✅ 悬停时光标变化应该与视觉边界一致

### 5. 查看控制台输出

打开浏览器开发者工具（F12），查看Console

**预期输出**:
```
车库Canvas尺寸: 1920 x 937
载具 闪电麦昆 的精确边界: 95x68px
载具 火箭飞船 的精确边界: 45x120px
调试边界显示: 已启用
```

## 📊 对比测试

### 之前的问题现象
❌ 载具离页面边界还有一段距离就反弹  
❌ 细长载具有很大的不可见碰撞区域  
❌ 点击空白处也会误选中载具  
❌ 两个载具之间有明显的间隙却碰撞了

### 现在的效果
✅ 载具紧贴页面边界（仅2px间隙）  
✅ 碰撞边界紧贴实际绘制内容  
✅ 点击检测精准，不会误触  
✅ 碰撞发生时边界实际接触

## 🎯 关键测试点

### 1. 边界贴合度
创建一个**极细的火箭**（宽度很小），观察：
- 是否能在水平方向自由移动直到真正碰到边界
- 纵向是否紧贴上下边界

### 2. 碰撞精准度
创建两个**L形载具**，观察：
- 它们的直角部分是否能够嵌套而不碰撞
- 只有实际内容接触时才发生碰撞

### 3. 性能测试
创建 **20+ 个载具**，观察：
- 动画是否流畅（应保持 60 FPS）
- 控制台是否有性能警告

## 🐛 可能的问题排查

### 问题1: 边界仍然偏大
**可能原因**: 浏览器缓存  
**解决方法**: 强制刷新（Ctrl+Shift+R 或 Cmd+Shift+R）

### 问题2: 调试模式无效
**可能原因**: 焦点不在车库界面  
**解决方法**: 点击车库画布后再按 D 键

### 问题3: 矩形边框不可见
**可能原因**: 载具图像加载未完成  
**解决方法**: 等待几秒后边界会自动更新

### 问题4: 控制台没有边界信息
**可能原因**: 前端未重启  
**解决方法**: 确认前端服务已经重启

## 📝 测试清单

- [ ] 启动前端服务
- [ ] 访问画个车页面
- [ ] 创建至少3个不同形状的载具
- [ ] 进入车库查看载具
- [ ] 按 D 键启用调试模式
- [ ] 验证矩形边界显示
- [ ] 检查控制台输出
- [ ] 测试页面边界贴合
- [ ] 测试载具间碰撞
- [ ] 测试点击/悬停检测
- [ ] 关闭调试模式（再按 D）

## ✅ 成功标准

1. **视觉**: 调试模式下蓝色矩形边框紧贴载具内容
2. **边界**: 载具能够贴近页面边缘（间隙 ≤ 2px）
3. **碰撞**: 载具间碰撞时边界实际接触
4. **交互**: 点击和悬停精准，无误触
5. **性能**: 20个载具时帧率 > 50 FPS

## 🎓 技术验证

### 检查边界数据
在控制台执行：
```javascript
// 获取第一个载具的边界信息
app.$children[0].$refs.garageCanvas
app.$children[0].vehicleSprites[0].boundingBox
// 应该看到: { width: 95, height: 68 }
```

### 检查碰撞检测方法
```javascript
// 查看当前使用的碰撞检测方法
console.log(app.$children[0].handleCollisions.toString())
// 应该包含 "AABB" 和 "overlapX"
```

## 📅 测试记录

**测试日期**: _____________  
**测试人员**: _____________  
**测试结果**: ✅ 通过 / ❌ 未通过  
**备注**: _____________

---

**提示**: 如果所有测试通过，可以关闭调试模式，正常使用了！


